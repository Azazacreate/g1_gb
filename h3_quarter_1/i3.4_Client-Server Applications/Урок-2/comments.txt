, отличное выполнение задания, справились со всеми пунктами ТЗ

. вопросы в ПР
также прошу вас посомтреть пример ДЗ

кроме того, при записи файлов нам нужно прописывать кодировку utf-8
иначе в файл попадают кракозябры

вопросы в ПР
жду вашу доработку в телеграм @cdi999
и оценка будет исправлена

я на уроке просил не удалять служебные файлы каждого задания, в том числе исходные и результирующие

, я просил не удалять ни служебные, ни результирующие файлы
пересдайте корретно

, второе задание нужно доработать
удаляете ключ orders, а этого делать нельзя 
я на уроке говорил

, в архиве не все материалы!

, корректный формат сдачи:  архив - в нем директория- в ней все файлы и папки
в третьем уроке задание в текущем виде принято не будет. спс за понимание

посмотрите пример как записать кириллицу

Обратная связь по заданиям:
1) выполнено
2) выполнено
3) выполнено

, вопросы в ПР


. успешного осв-я. Если что. я на связи

, задание выполнено в соответствии с ТЗ
если вам требуется моя помощь, телеграм @cdi999

, пока формальное принятие

Обратная связь по заданиям:
1) выполнено, но нужно явно указывать ютф-8, иначе - кракозябры. плюс не убраны пустые строки
2) выполнено, но нужно явно указывать ютф-8, иначе - кракозябры. плюс не убраны пустые строки
3) не довыполнено, в файл записываются кракозябры вместо символов

Обратная связь по заданиям:
1) выполнено, но нужно явно указывать ютф-8, иначе - кракозябры. плюс не убраны пустые строки
2) выполнено, но нужно явно указывать ютф-8, иначе - кракозябры
3) выполнено, но не применен вариант с кодовыми точками

Обратная связь по заданиям:
1) выполнено
2) выполнено, но не попытались записать кириллицу. посмотрите пример
3) выполнено, но не применен вариант с кодовыми точками + нужно явно указывать ютф-8, иначе - кракозябры


Обратная связь по заданиям:
1) выполнено
2) выполнено
3) выполнено, но желательно представить yaml-файл в более читабельном виде, как я показал на уроке

Обратная связь по заданиям:
1) выполнено, но не убраны пустые строки
2) выполнено, но не попытались записать кириллицу. посмотрите пример
3) выполнено, но не применено самое главное,
 для чего давалось это задание - вариант с кодовыми точками. плюс у вас не указана кодировка utf-8
поэтому в файле кракозябры

жду вашу доработку в телеграм @cdi999
и оценка будет исправлена

Даниил, минус балл за неверную сдачу задания

ВНИМАНИЕ!!: сдача задания
1) создать папку Lesson_1_Ivanov
2) в папку положить файлы task_1 - task_6 (а также txt-файл для последнего)
3) заархивировать папку! и сдать архив

в след раз неверная сдача - минус два балла. формат сдачи был обозначен еще в уроке 1

1) при открытии файла пяовляются кракозябры. не указана верная кодировка
2) выполнено, но не попытались записать кириллицу
3) f,default_flow_style=False, - ошибка стиля.